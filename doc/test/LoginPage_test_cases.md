

> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。
> 測試類型：UI 呈現、表單驗證、API 互動、路由導轉...

---

## [x] 【UI 呈現】檢查登入頁面基本元素
**範例輸入**： 渲染 LoginPage 元件
**期待輸出**： 
1. 包含標題「歡迎回來」
2. 包含 Email 輸入框
3. 包含密碼輸入框
4. 包含登入按鈕
5. 包含測試帳號提示資訊 (非 API URL 環境)

---

## [x] 【表單驗證】驗證無效的 Email 格式
**範例輸入**： 在 Email 輸入框輸入 "invalid-email"，點擊登入
**期待輸出**： Email 輸入框下方顯示錯訊訊息「請輸入有效的 Email 格式」

---

## [x] 【表單驗證】驗證密碼長度不足
**範例輸入**： 在密碼輸入框輸入 "1234567" (7 碼)，點擊登入
**期待輸出**： 密碼輸入框下方顯示錯誤訊息「密碼必須至少 8 個字元」

---

## [x] 【表單驗證】驗證密碼未包含英文字母與數字
**範例輸入**： 在密碼輸入框輸入 "12345678" (僅數字) 或 "abcdefgh" (僅字母)
**期待輸出**： 密碼輸入框下方顯示錯誤訊息「密碼必須包含英文字母和數字」

---

## [x] 【API 互動】驗證登入失敗處理
**範例輸入**： 輸入有效的 Email 與密碼，Mock login API 回傳 401 錯誤 (message: "帳號或密碼錯誤")
**期待輸出**： 頁面顯示錯誤 Banner，內容為「帳號或密碼錯誤」

---

## [x] 【API 互動】驗證登入成功導轉
**範例輸入**： 輸入有效的 Email 與密碼，Mock login API 回傳成功
**期待輸出**： 
1. `login` function 被呼叫
2. 頁面導轉至 `/dashboard`

---

## [x] 【UI 呈現】驗證 Loading 狀態
**範例輸入**： 點擊登入按鈕，等待 API 回應期間
**期待輸出**： 
1. 登入按鈕進入 disabled 狀態
2. 按鈕文字變為「登入中...」

---

## [x] 【路由導轉】驗證已登入狀態自動導轉
**範例輸入**： 設定 `isAuthenticated` 為 `true`，渲染 LoginPage
**期待輸出**： 頁面自動導轉至 `/dashboard`

---

## [x] 【邏輯處理】驗證 Auth Expired 訊息顯示
**範例輸入**： 設定 `authExpiredMessage` 為 "請重新登入"，渲染 LoginPage
**期待輸出**： 
1. 頁面顯示錯誤 Banner，內容為 "請重新登入"
2. `clearAuthExpiredMessage` 被呼叫
